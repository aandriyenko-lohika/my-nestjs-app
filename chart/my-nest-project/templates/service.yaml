apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-nest-project.fullname" . }}
  labels:
    {{- include "my-nest-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "my-nest-project.selectorLabels" . | nindent 4 }}

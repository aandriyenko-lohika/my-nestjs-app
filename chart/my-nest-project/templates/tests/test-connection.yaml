apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-nest-project.fullname" . }}-test-connection"
  labels:
    {{- include "my-nest-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-nest-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
